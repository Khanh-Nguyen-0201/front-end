import React, { Component } from 'react';
import {Link} from 'react-router-dom';
import {addCompareProduct} from "../../_actions/compare.action";

export class ProductComponent extends Component{
    state = {};
    render() {
        const {product} = this.props;
        return{

        }
    }
}